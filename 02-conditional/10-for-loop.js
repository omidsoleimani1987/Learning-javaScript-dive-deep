// for loop
for (let i = 4; i <= 1000000; i = i * 15) {
  // console.log(i);
}

for (let i = 0; i < 10; ) {
  i++; // condition can be inside too
  console.log(i);
}
// infinite loop --> for (;;) { // some action here }

// for-of loop:
// execute for every element in an array

// for-in loop:
// execute for every key in an object
