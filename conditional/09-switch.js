// switch always uses === to comparing the values
switch (event) {
  case 'click':
    //do something
    break;
  case 'load':
    // do another thing
    break;
  default:
  //do something by default if none of the cases match
}
